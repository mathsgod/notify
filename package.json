{
  "name": "notify",
  "version": "1.0.0",
  "description": "Monitor endpoints and send notifications based on conditions",
  "main": "dist/bundle.cjs",
  "bin": {
    "notify": "./dist/bundle.cjs"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "esbuild index.js --bundle --platform=node --target=node18 --outfile=dist/bundle.cjs",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.14.0",
    "commander": "^14.0.3",
    "nodemailer": "^8.0.4"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
